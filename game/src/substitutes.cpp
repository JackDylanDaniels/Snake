#include "substitutes.h"
int min(int a, int b) {
    return (a < b) ? a : b;
}
int strlen(const char* str) {
    int length = 0;
    while (str[length] != '\0') {
        length++;
    
}
    return length;
}
